# Sintaxe Básica e Variáveis em Go

Go é uma linguagem fortemente tipada, o que traz mais segurança para o código, mas sua sintaxe é desenhada para ser limpa.

## Declaração de Variáveis
Existem duas formas principais de declarar variáveis:

1. **Declaração Padrão (`var`)**:
   Utilizada quando você quer definir o tipo explicitamente ou declarar sem atribuir valor imediato.
   ```go
   var nome string = "Arthur"
   var idade int = 19

   Go
cidade := "Osasco" // O Go entende que é uma string

## Tipos de Dados
* **string**: Para textos.
* **int**: Para números inteiros.
* **float64**: Para números com casas decimais.
* **bool**: Valores booleanos (`true` ou `false`).

## Constantes
Para valores que não podem ser alterados durante a execução do programa:
```go
const Professor = "Impacta"



